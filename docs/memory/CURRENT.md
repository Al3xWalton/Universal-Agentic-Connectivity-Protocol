# CURRENT — where UACP is right now

**Last updated**: 2026-05-06 — **Stage 11.2 — operation synthesis from captures shipped (3 prototype commits + memory commit; no tag).** Closes the §3.12 capture pipeline against the v1.1.0-frozen spec: a captured session (Stage 11.1) becomes a draft `.uacp` file via deterministic clustering + LLM synthesis, gated on explicit user approval per §3.12 + §3.8 mandatory-user-review. The brief's headline framing — "AVA learns by watching the user" — is concrete end-to-end: capture → analyze → synthesize → review → persist → validate. **Spec repo commit chain on top of `42012cc` (Stage 11.1 memory tip)**: `4c066f1 feat(prototype): capture analyzer — clustering + parameter inference` → `9c69c1f feat(prototype): LLM synthesis pipeline from capture artifacts` → `c0dd056 feat(prototype): synthesize-from-capture CLI + user-review surface`. Tip will be this memory commit; **no tag** — Stage 11.2 is implementation against the frozen v1.1.0 spec, not a spec amendment. **Module structure**: `prototype/python/src/uacp_prototype/capture/analyzer.py` (deterministic clustering layer); `prototype/python/src/uacp_prototype/connections/ingest_capture.py` (LLM synthesis pipeline mirroring `ingest_nl.py`'s structure); CLI extension to `cli.py` (synthesize-from-capture subcommand + interactive review surface). New pydantic model `CaptureSource` in `spec/models.py` (added in v1.1.0 schema; this stage lands the matching pydantic surface) + new `_validate_capture_provenance` pass in `spec/schema.py` enforcing the §3.12 mandatory-user-review rule below the CLI. **Analyzer (capture/analyzer.py)** — deterministic clustering algorithm per §3.12: variable-shape recognizers (UUID / integer / hex token ≥12 / slug-with-digits / email; pure-alpha tokens stay literal); path-signature clustering replacing variable-shaped segments with `:var` and grouping by (method, signature); path-parameter naming with priority (a) preceding-literal-segment singularized to `<thing>_id` at confidence 0.85, (b) shape-derived (`uuid_N` / `id_N` / `slug_N` / `email_N` / `hex_N`) at 0.55, (c) generic `param_N` at 0.30; `ParameterFrequency` table for query parameters + non-auth request headers + JSON/form body keys with `seen / total` counts and `is_required` derived from 100% frequency; body-shape ambiguity flag set when bodies overlap <80% (Jaccard); noise filtering (third-party hosts relative to the primary host = the host with most requests; image/font/CSS/JS/audio/video Content-Type prefixes; favicon / manifest / service-worker / robots.txt; OPTIONS preflights). Auth-artifact extraction surfaces cookie names (from storage_state) + Authorization-header counts + CSRF-header counts — NEVER raw token values. Authorization / Cookie / X-API-Key request headers stripped from candidate operations' `request_headers` (auth plumbing belongs in §2.10 connection blocks, not in operation parameter schemas). The brief's hard rule "the clustering algorithm in §1.2 is deterministic; don't sneak LLM calls into it" is mechanical: the module imports nothing from `connections/ingest_*` and makes no network calls. Speed target: <100ms per artifact analysis (verified with a 100-entry synthetic capture; analysis completes in ~10ms; the LLM call dominates real sessions). **Synthesis pipeline (connections/ingest_capture.py)** — mirrors `ingest_nl.py`'s structure for architectural consistency. `synthesize_from_capture(capture_ref, user_intent, llm)` loads + decrypts the artifact, runs `analyze_capture` deterministically, builds an LLM prompt embedding the AnalysisResult's structured summary (the LLM never sees raw HAR), parses the JSON response, drops hallucinated operations not in the candidate-cluster set, returns a `CaptureSynthesisDraft` with §3.12 provenance (source.type=capture / captured_at / user_intent / capture_ref / confidence; reviewed_at unset until confirmation). `_filter_to_candidates` enforces the brief's hard rule "an LLM that returns operations beyond the candidate list should have those operations dropped at validation" mechanically — even if the LLM ignores the system-prompt instruction, the (method, path_template) match drops extras into `draft.dropped_operations` for visibility but never into the persisted file. `refine_synthesis` runs the refinement loop with hard 3-iteration cap (DEFAULT_MAX_REFINEMENT_ROUNDS=3) per the brief's "prevents runaway LLM-back-and-forth" affordance; each refinement preserves the prior draft's operation ids per §3.12 + §3.8 stability rule. `confirm_and_persist` stamps `reviewed_at` (RFC 3339) and writes the .uacp file; per §3.12 + §3.8, `approved=True` is required, anything else raises `SynthesisNotApprovedError`. Default `$schema` URL is the v1.1.0 tag. **CLI (synthesize-from-capture)** — `--capture-ref <secret://> --intent "<NL>" --output FILE [--force]`. Validates the secret-URI shape, refuses to overwrite existing output without `--force`, rejects empty intent. Renders the draft to the terminal (id / summary / method+path / idempotency / params with required/optional flags / provenance) and surfaces the LLM's hallucinated-and-dropped operations separately for transparency. The interactive review prompt accepts `a`/`approve`, `e`/`edit`, `r`/`refine`, `x`/`abort` (single-letter or word-form via `_DECISION_ALIASES`); loops on unrecognized; EOF treats as abort to prevent silent persistence. Edit path opens `$EDITOR` (overridable via `editor_command` for tests) on a tempfile; on save, the edited file replaces the in-memory draft with `reviewed_at` cleared so `confirm_and_persist` re-stamps. Refine path catches `RefinementLimitExceeded` after 3 rounds with a clear "switch to manual editing" message. Approve path calls `confirm_and_persist(approved=True)` + emits §6.6 file-persisted audit event. **Auth-block selection design decision**: synthesis produces the operations block; `authentication: {}` ships empty in the synthesized file and the operator fills in the matching auth block (typically `session_cookie` per §2.10 for grey-zone providers, or one of the OAuth methods) before the file passes `uacp validate`. Auth-block inference from `auth_artifacts` is a future-v1.x candidate — Stage 11.2 stops at the operations level per the brief's "Don't implement [auth selection]; that's Stage 11.2 [scope is operations]" framing. **Spec-level enforcement of §3.12 mandatory-user-review**: `spec/models.py` gains `CaptureSource` (type=capture, captured_at, user_intent, capture_ref, confidence?, reviewed_at — all non-empty per field-validator; capture_ref MUST be a secret:// URI per §2.7); `spec/schema.py` gains `_validate_capture_provenance` mirroring `_validate_inferred_provenance` — surfaces a uniform `SpecValidationError` when any of (captured_at / user_intent / capture_ref / reviewed_at) is missing or empty. The brief's hard rule "loading code in spec/loader.py rejects any .uacp file with source.type: 'capture' AND missing source.reviewed_at" lives below the CLI now, not just at the UX prompt. **§6.6 audit emission** at four lifecycle points: synthesis-started (capture_ref + candidate count + intent_len — never the intent text itself), synthesis llm-call-completed (kept_ops + dropped_ops + raw_chars), synthesis user-reviewed (decision letter), synthesis file-persisted (output_path + operation_count). Audit payloads NEVER carry the operator's full intent text or any captured cookie / Authorization values — the recorder + analyzer's auth-value scrubbing posture extends through the synthesis layer (verified by `test_synthesis_audit_does_not_log_user_intent_verbatim`). **Live demonstration on this machine**: captured https://httpbin.org/anything?demo=stage11-2 via Stage 11.1's CLI (1 request, 5.2s, mode-0600 AES-256-GCM blob); ran synthesize-from-capture with a recorded mock LLM (no real API key configured). The analyzer correctly clustered the single captured request into one candidate operation (`GET /anything` with `demo` query parameter); the mock LLM produced a UACP operation entry `id=inspect_request summary="Echoes the request method, headers, and query parameters via httpbin.org/anything."` with the demo parameter as an optional query parameter; the review flow accepted it (`a`); the persisted file (after operator-supplied auth block per the design decision) loads cleanly through `uacp validate` with `authentication=api_key_header, base_url=https://httpbin.org, 1 operation idempotency=idempotent`. **Honest read on synthesis quality**: the prototype's pipeline is structurally sound — analyzer clustering is deterministic and tested, hallucination-drop is mechanical, persistence-requires-approval is enforced spec-side. The synthesis QUALITY against real LLM responses is operator-driven post-session (no API key in this environment); the architecture lets operators swap `_build_capture_llm` for any LLMCallable. The recorded mock-LLM response in the live demo is realistic but not adversarial — operator validation against e.g. `anthropic/claude-haiku-4.5` is the next quality gate. The brief's stop condition "the LLM synthesis quality on real captures is poor enough that user approval would be impractical" was not exercised; we exit DONE on the structural surface and flag operator-driven LLM-quality validation as the follow-up. **Test counts after Stage 11.2**: **596 unit tests passing** (508 Stage 11.1 baseline + 40 analyzer + 22 synthesis + 26 CLI = 88 new), **1 skipped** (the playwright-not-installed self-skip), **36 integration tests deselected by default** (25 provider + 8 MCP + 1 scrapling + 2 capture_integration). 0 failures. Run wall ~0.70s. **Hard rules honored**: synthesis pass MUST NOT persist without explicit user approval — verified at three layers (CLI's review prompt; ingest_capture.confirm_and_persist's approved=True gate; spec/schema._validate_capture_provenance rejecting capture .uacp files missing reviewed_at). Clustering algorithm is deterministic — no LLM imports in capture/analyzer.py; tests verify same-input-same-output. The prompt instructs the LLM not to hallucinate AND the filter mechanically drops hallucinations beyond the candidate list — verified by `test_synthesize_drops_operations_not_in_candidate_list`. Did not update the v1.1 spec — implementation against the frozen spec only. Did not push the repo. The refinement loop has hard cap of 3 — verified by `test_refine_synthesis_caps_at_three_rounds`. **Spec repo state after this session**: tip will be this memory commit; three new prototype commits + this memory commit. New tree under `prototype/python/`: `src/uacp_prototype/capture/analyzer.py` (NEW); `src/uacp_prototype/connections/ingest_capture.py` (NEW); `tests/unit/{test_capture_analyzer,test_ingest_capture,test_synthesize_cli}.py` (NEW). Modified: `src/uacp_prototype/capture/__init__.py` (re-exports); `src/uacp_prototype/cli.py` (new subcommand + helpers); `src/uacp_prototype/spec/models.py` (CaptureSource pydantic model); `src/uacp_prototype/spec/schema.py` (capture-provenance validation pass); `prototype/python/README.md` (new "Operation synthesis from captures (v1.1)" section). **Operator action items at session end**: push the three prototype commits + this memory commit to `origin/main` (on top of `42012cc`). No tag — Stage 11.2 is implementation against the frozen v1.1.0 spec.

---

**Prior session (2026-05-05)**: **Stage 11.1 — browser capture infrastructure shipped (3 spec-repo commits + memory commit; no tag).** Implements the recording side of §3.12 against the v1.1.0-frozen spec: a CLI-driven flow that opens the user's target service in a real browser via Playwright, records every HTTP request the browser makes during the session as HAR 1.2-format entries, captures the post-login `storage_state` (cookies + localStorage), and persists the result as an AES-256-GCM-encrypted blob under §6.3 envelope encryption with a stable `secret://local-keyring/<id>` reference for Stage 11.2's operation-synthesis pass to consume. **Spec repo commit chain on top of `2b1ba64` (Stage 11.0 memory tip + v1.1.0 tag)**: `f0b0ad8 feat(prototype): browser capture recorder (Playwright + Scrapling backends)` → `ead0ceb feat(prototype): encrypted-at-rest capture storage` → `7962c24 feat(prototype): uacp capture-session CLI command`. Tip will be this memory commit; **no v1.x tag** — Stage 11.1 is a prototype-only extension on top of the frozen v1.1.0 spec, not a spec amendment. **Module structure**: new `prototype/python/src/uacp_prototype/capture/` subpackage with `__init__.py` (re-exports), `recorder.py` (BrowserRecorder + PlaywrightBackend + ScraplingBackend + CaptureArtifact + HarEntry + checkpoint resilience + audit emit), `storage.py` (store_capture / load_capture composing on the existing LocalKeyringStore for §6.3 envelope encryption). New top-level CLI command `uacp capture-session --initial-url <url> --output <secret-ref> [--browser playwright|scrapling] [--provider <name>]`. **Backend selection**: PlaywrightBackend is the default (lazy-imports `playwright.sync_api` from the new optional `capture` extras group). ScraplingBackend per the brief's explicit fallback affordance ("Scrapling's browser API is dispatch-only as of v0.3") transparently delegates to PlaywrightBackend after verifying the `stealth` extras are installed; logs an INFO note that the captured fingerprint is Playwright's rather than Scrapling's. Stage 11.2+ may revisit when Scrapling exposes a session-capable browser API. Stage 11.0's ScraplingTransport for *dispatch* still uses Scrapling's stealth posture for replay; capture-and-replay through different fingerprints is a known limitation flagged in the prototype README. **Capture artifact format**: `CaptureArtifact` carries `capture_id` (deterministic SHA-256 of `initial_url + captured_at + provider`, truncated to 16 hex chars per §3.12), `captured_at`, `browser_backend`, `initial_url`, `final_url`, `entries: list[HarEntry]` (each with `started_at`, `time_ms`, `request: {method, url, headers, body}`, `response: {status, status_text, headers, body}`), `storage_state: dict | None`, `metadata: dict`. `to_har_json()` emits HAR 1.2-spec-compliant output (so standard HAR tools can inspect after explicit decryption); `to_internal_json()` includes the `storage_state` + capture-id + UACP metadata for the persistence path. The HAR shape uses canonical camelCase field names (`startedDateTime`, `headersSize`, `bodySize`, `postData`); the internal shape uses snake_case Python idioms. **Encryption-at-rest at the persistence boundary**: `store_capture()` composes on `security/secrets.LocalKeyringStore.put()` which already does §6.3 envelope encryption (per-blob DEK wrapped by master KEK at `~/.uacp/master.key`; AES-256-GCM ciphertext on disk, mode 0600 with 0700 parent dir). The new `storage_id` kwarg lets the CLI honor the operator's `--output` URI verbatim while keeping auto-id-derivation as the default for programmatic callers. `load_capture()` rounds out the read side for Stage 11.2's synthesis pass. The §3.12 mandate "MUST be encrypted before persistence" is satisfied by reuse rather than reinvention. **Verified end-to-end** on this machine: the live demonstration ran `uacp capture-session --initial-url https://httpbin.org/anything?demo=stage11 --output secret://local-keyring/stage11-demo-capture` against a real Playwright-headed Chromium (run headless for the non-interactive terminal); recorded 1 request in 6.7s; persisted as a 3189-byte 0600-mode AES-256-GCM blob at `~/.uacp/secrets/stage11-demo-capture.enc` whose ciphertext does not contain the captured URL or response body; `load_capture()` round-trips back to `capture_id=b16994de20ac5002` with the GET against httpbin (status 200) plus the storage_state. **Resilience**: every 30s the recorder flushes its in-progress capture to `~/.uacp/captures/in-progress/<id>.har.tmp` (mode 0600, plaintext, deleted on clean stop). `recover_in_progress(capture_id)` reads the checkpoint after a mid-session crash; the CLI's signal handlers (SIGINT/SIGTERM) mark the recorder's disconnect_event so the await loop exits cleanly and partial artifacts persist. Browser-disconnect detection wires through Playwright's `browser.on("disconnected")` event into a `threading.Event` the CLI's await loop polls alongside stdin. **§6.6 audit emission** at three lifecycle points: `capture started: id=<id> backend=<backend> initial_url=<url>`, `capture stopped: id=<id> requests=<count> duration_ms=<elapsed>`, `capture stored: ref=<secret://...> id=<id> requests=<count> duration_ms=<elapsed> host=<initial_host>`. Audit payloads carry only URL pattern + counts + duration — never captured cookies / `Authorization` headers / `storage_state` values. The `_scrub_for_audit()` helper redacts known auth-bearing header names case-insensitively (Authorization, Cookie, Set-Cookie, X-API-Key, X-Csrf-Token, X-XSRF-TOKEN, X-Goog-AuthUser, etc.) for any future audit-emit path that needs to log header maps. `_audit_url_pattern()` strips query strings + replaces numeric path segments with `:id` for audit-event grouping. **CLI orchestration**: `_validate_secret_ref()` parses the `--output` URI (rejects missing scheme / unknown store / `#field` selector with exit code 2). `_await_user_signal_or_browser_close()` blocks until Enter is pressed (returns `user_stop`) or the browser disconnects (returns `browser_closed`); prints live progress every 5s; tests inject mocked stdin/stdout/sleep/time so the loop is deterministic. Disconnect check fires BEFORE Enter check so the disconnect path takes precedence in the EOF-vs-disconnect race the empty-stdin test fixture creates. The orchestration handles unreachable initial URLs (CaptureError → exit code 3, no empty artifact stored), stop failures (exit code 4), persistence failures (exit code 5). Signal handlers restore prior dispositions before stop() runs so cleanup errors don't loop back through the handler. **Test counts after Stage 11.1**: **508 unit tests passing** (452 v1.1.0 baseline + 27 recorder tests + 14 storage tests + 16 CLI tests = 57 new — but 1 self-skips when the `capture` extras are installed because the test asserts the not-installed remediation path), **1 skipped** (the self-skip just mentioned), **36 integration tests deselected by default** (25 provider + 8 MCP + 1 scrapling-marked + 2 NEW capture_integration). 0 failures. Run wall ~0.74s. Both capture_integration tests pass against real Playwright + httpbin. **The `pyproject.toml` adds**: optional `capture` extras group with `playwright>=1.45`; new `capture_integration` pytest marker (skipped by default via addopts; opt-in with `pytest -m capture_integration` after `uv sync --extra capture && uv run playwright install chromium`). **Hard rules honored**: did not implement operation synthesis (Stage 11.2 territory — explicitly deferred per the brief); captured artifacts MUST be encrypted at rest with no plaintext fallback (verified via `test_persisted_blob_is_encrypted_at_rest`); audit events scrub auth values (verified via `test_capture_stored_audit_event_does_not_leak_auth` + `test_audit_payloads_do_not_log_auth_headers`); default backend is Playwright (Scrapling is optional and falls back); did not change Stage 11.0's spec; did not push the repo or any tag; did not write the operation synthesis flow despite the temptation to start sketching. **Spec repo state after this session**: tip will be this memory commit; three new prototype commits + this memory commit. New tree under `prototype/python/`: `src/uacp_prototype/capture/{__init__.py,recorder.py,storage.py}`; `tests/unit/{test_capture_recorder.py,test_capture_storage.py,test_capture_cli.py}`; `tests/integration/test_capture_session_live.py`. Modified: `src/uacp_prototype/cli.py` (new capture-session subcommand + helpers), `pyproject.toml` (capture extras + capture_integration marker), `README.md` (new "Session capture (v1.1)" section). **Operator action items at session end**: (1) push the local commits to `origin/main` (three prototype commits + this memory commit on top of `2b1ba64`). No tag this session — Stage 11.1 is implementation against the frozen v1.1.0 spec, not a spec amendment.

---

**Prior session (2026-05-05)**: **Stage 11.0 — v1.1 spec amendment + Scrapling-based pluggable transport shipped (6 spec-repo commits + memory commit + v1.1.0 git tag).** First non-breaking minor release on top of the `v1.0.0` freeze. Strengthens the protocol's universality story before Stage 10 (production reference implementation in AVA's monorepo) starts: §3.12 establishes a fourth schema source (browser-demonstrated session captures) for providers without published APIs, and §4.10 codifies the conformance posture for substituting HTTP transport backends — particularly anti-bot-evading backends for `session_cookie` connections to fingerprint-defended providers. Both amendments are non-breaking per §7.2 (additive only); every v1.0-valid artifact continues to validate against v1.1 — verified by running all 10 example artifacts through the v1.1 schema. **Spec repo commit chain on top of `fc3d923` (v1.0.0 tag commit / Stage 9 memory tip)**: `f9b72be feat(spec): §3.12 — session_capture schema source (v1.1)` → `1b0e9fe feat(spec): §4.10 — pluggable transport backends (v1.1)` → `7c5db9f feat(spec): v1.1 status flags + SPEC.md update` → `39ed970 feat(prototype): pluggable transport abstraction (httpx + Scrapling)` → `231e08a feat(prototype): wire ScraplingTransport for session_cookie connections` → `3d300bf docs: v1.1.0 release notes`. Tip will be this memory commit; `v1.1.0` tag created locally pointing at the memory commit and not pushed. **§3.12 Session capture** placed at the end of §3 to preserve the v1.0 numbering of §3.6 — §3.11; logically sits between §3.8 (LLM inference) and §3.9 (source priority). Subsections: overview (capture as the empirical-floor source for unspecified providers), capture format (HAR-recommended canonical shape with variable-header flagging + size-truncation rules), operation inference rules (clustering by endpoint+method, path-parameter extraction by URL pattern matching, parameter-required by frequency, LLM refinement per §3.8), provenance metadata (source.type=capture + captured_at + user_intent + capture_ref + reviewed_at), user review (MUST gate parallel to §3.8), capture infrastructure (Playwright / DevTools / mitmproxy all conforming), conformance (MAY support; capture-artifact-must-persist; load-time MUST recognize source.type=capture even when not authoring). **§3.9 priority order amended**: 1. user > 2. curl > 3. captured session (NEW) > 4. OpenAPI > 5. LLM-inferred. Capture outranks OpenAPI because the captured request is what the live service actually accepted at the moment of capture, not the provider's self-description. **§3.11 conformance summary** gains a session_capture row (MAY level) plus a new MUST NOT entry mirroring §3.8's review gate. **§4.10 Pluggable transport backends** codifies the conformance posture under which an implementation MAY substitute HTTP client libraries per Connection / per auth method / per artifact. Hard rule: backend swap permitted ONLY when §4.1 — §4.9 contract preserved. Anti-bot evasion is not an excuse for skipping retries (§4.3), ignoring rate limits (§4.5), dropping audit logs (§6.6), or surfacing non-canonical errors (§4.6) — Provider-respect-norms don't bend just because the request shape mimics a browser. Selection mechanism: implementations MAY default to a stealth backend for session_cookie connections (auth-method affinity); artifacts MAY override via the new optional `dispatch.transport` field. Recommended values: "default", "stealth", or `x-`-namespaced implementation identifiers per §7.3. Implementations that don't recognize the named transport MUST fall back to default with a warning rather than refuse to dispatch — graceful-degradation posture preserves the round-trip rule of §3.11. **schemas/uacp.json** $id bumped to `v1.1.0`; title/description updated; Source oneOf gains a fourth variant for type=capture; DispatchConfig gains an optional `transport` property with the recommended-or-x-namespaced pattern. **SPEC.md retitled v1.1.0** with Status section rewritten to acknowledge v1.0.0 as the baseline + v1.1.0 as the first non-breaking minor; canonical $schema URL pinned at v1.1.0/schemas/uacp.json (the v1.0.0 URL continues to resolve indefinitely as a forward-compatible reference); §3 + §4 rows in the reading-order table updated to call out the §3.12 + §4.10 additions. **00-primer.md Status section** flipped from v1.0.0 to v1.1.0 with the same non-breaking-extension framing. **Prototype implementation**: new module at `prototype/python/src/uacp_prototype/dispatch/transport.py` exposing the Transport Protocol (minimal `request(method, url, *, headers, content, timeout) -> httpx.Response` + `close()` surface so the dispatch loop's response-handling code stays transport-neutral); HttpxTransport (default backend, wraps httpx.Client, identical pre-v1.1 behavior); ScraplingTransport (optional anti-bot-evading backend backed by Scrapling's StealthyFetcher / Camoufox-driven, lazy-imports Scrapling so prototype runs without the optional `stealth` extras, adapts Scrapling's response shape to httpx.Response, re-raises backend errors as httpx.HTTPError per §4.10's "Backend-specific exception types MUST NOT leak past the dispatch boundary" rule, refuses non-HTTPS URLs at the transport boundary as defense-in-depth); ScraplingNotInstalledError with clear remediation message; `is_scrapling_available()` helper; `select_transport_for_artifact(artifact)` implementing the §4.10 decision tree. **dispatch/client.py** refactored to route requests through `self._transport`; `httpx_client=` parameter kept as back-compat alias (wrapped into HttpxTransport implicitly); new `transport=` parameter takes precedence; small `_TransportGetAdapter` wraps the transport for auth helpers (e.g. session_cookie's refresh_csrf) that expect a `.get(url, headers)`-shaped client — keeping the §4.10 posture coherent across dispatch + refresh paths. **dispatch/pagination.py** link-header next-page request also routes through `client.transport`. **MCP server's `build_dispatch_client_default` factory** calls `select_transport_for_artifact(artifact)` and passes the result to DispatchClient; the prototype's MCP composition surface honors the same selection rules. **NotebookLM example artifacts** (examples/notebooklm/list-notebooks.uacp + send-chat-message.uacp) updated with `dispatch.transport: "stealth"` to declare their affinity explicitly. **pyproject.toml** gains the optional `stealth` dependency group (`scrapling>=0.3`) + a new `scrapling` pytest marker (skipped by default; opt-in via `pytest -m scrapling`). **prototype/python/README.md** NotebookLM section gains the `uv sync --extra stealth` step + a description of the trade-offs (best-effort, §2.10 ToS + §6.6 audit obligations preserved regardless of which transport carries the bytes); MCP composition section gains a "Transport selection (v1.1)" subsection documenting the four-step decision tree. **`prototype/python/tests/unit/test_transport.py`** ships 18 tests covering Protocol membership, HttpxTransport behavior (respx-verified), default-selection, back-compat httpx_client= alias, ScraplingTransport graceful failure when extras missing, .uacp transport-field round-trip, HTTPS-only at the transport boundary, the §4.10 selection decision tree (explicit "default", explicit "stealth" fallback when extras missing, unknown identifier graceful fallback, session_cookie auth-method affinity when extras missing, NotebookLM-examples-declare-stealth fixture verification). One test marked `@pytest.mark.scrapling` runs only when extras are installed. **Test count after Stage 11.0**: **452 unit tests passing** (434 v1.0.0 baseline + 18 transport tests), **34 integration tests deselected by default** (25 provider integration + 8 MCP integration + 1 scrapling-marked test), 0 failures. Run wall ~0.62s. **All 10 example .uacp files validate** against the v1.1 schema (forward-compat verified). **Word counts**: §3.12 ≈ 870 words, §4.10 ≈ 740 words; SPEC.md 918 words; RELEASE-v1.1.0.md 1251 words. **Spec gaps surfaced**: ZERO. v1.1 is purely additive — every v1.0 conformance rule is preserved, every new section MAY-level. **Hard rules honored**: spec amendments are additive (no §3.6/§3.7/§3.8 substance changed; only cross-references updated); Scrapling integration is OPT-IN (default behavior unchanged when extras not installed); existing tests pass without Scrapling installed; capture flow implementation deferred to Stage 11.1+ as the brief required; v1.0 commits stay frozen (v1.1 built on top); §4.10 explicitly preserves §4.1 — §4.9 conformance for any transport (anti-bot evasion does not excuse retries / rate limits / audit logs / canonical errors); did not push the repo or tag. **Spec repo state after this session**: tip will be this memory commit; six new commits + this memory commit + the v1.1.0 git tag. New tree: `schemas/uacp.json` revision (Source variant + DispatchConfig.transport); `prototype/python/src/uacp_prototype/dispatch/transport.py` (NEW); `prototype/python/tests/unit/test_transport.py` (NEW); `RELEASE-v1.1.0.md` (NEW). Modified: `docs/03-schema.md`, `docs/04-dispatch.md`, `docs/00-primer.md`, `SPEC.md`, `prototype/python/src/uacp_prototype/dispatch/client.py`, `prototype/python/src/uacp_prototype/dispatch/pagination.py`, `prototype/python/src/uacp_prototype/mcp/server.py`, `prototype/python/pyproject.toml`, `prototype/python/examples/notebooklm/{list-notebooks,send-chat-message}.uacp`, `prototype/python/README.md`. **Operator action items at session end**: (1) push the local commits to `origin/main`; (2) push the `v1.1.0` tag (`git push origin v1.1.0`); (3) optionally create a GitHub release for v1.1.0 using the drafted notes from `RELEASE-v1.1.0.md`.

---

**Prior session (2026-05-04)**: **Stage 9 — v1.0 spec freeze + MCP composition validation shipped (5 spec-repo commits + memory commit + v1.0.0 git tag).** Closes the design phase. Pins the canonical `$schema` URL at the v1.0.0 git tag (`https://raw.githubusercontent.com/Al3xWalton/Universal-Agentic-Connectivity-Protocol/v1.0.0/schemas/uacp.json`), publishes the canonical JSON Schema 2020-12 artifact at `schemas/uacp.json`, finalizes SPEC.md / 00-primer / README / GOVERNANCE for the v1.0.0 release, and demonstrates Principle 4 (composability with MCP) end-to-end via a thin MCP server adapter at `prototype/python/src/uacp_prototype/mcp/`. **Spec repo commit chain on top of `df4c2ef` (Stage 8e memory tip)**: `827e823 feat(spec): canonical $schema URL + schemas/uacp.json artifact` → `638579d feat(spec): v1.0 freeze — finalize SPEC.md, README, GOVERNANCE, per-stage status` → `125da94 feat(prototype): MCP server wrapper exposing UACP operations as MCP tools` → `c01bbcb test(prototype): MCP composition validation against MCP Python SDK` → `0a3f4be docs: MCP composition section in prototype README + GitHub release notes draft`. Tip is `0a3f4be` until this memory commit lands; tag `v1.0.0` is created locally on this memory commit and not yet pushed. **The session's central find**: §3.10's placeholder `$schema` URL was the load-bearing question gating freeze; the spec repo path `Al3xWalton/Universal-Agentic-Connectivity-Protocol` plus the v1.0.0 git tag provides a canonical URL that's free, stable, version-pinned, and matches §7.1's existing MAJOR.MINOR-bumping convention (each future minor release publishes a new tag URL). **The canonical schema artifact** at `schemas/uacp.json` is JSON Schema 2020-12 covering top-level structure (authentication / dispatch / operations / definitions / encrypted_secrets); the ten registered authentication methods per §2.1 with method-specific shape constraints in `allOf`/`if-then` blocks (including the §2.10 session_cookie + literal-true `tos_acknowledged` rule); request method/path/parameters/body shapes per §3.2 (with the path-no-query rule expressed as `not: pattern: \?`); response keys (3-digit status, Nxx range, default) per §3.3; the body format discriminator (json/xml/binary/text) per §3.3 Stage 8c; the FailurePredicate per §3.3 + §4.6 Stage 8b; the four pagination patterns per §3.4; the three source provenance shapes (openapi/curl/inferred) per §3.5–§3.8; the `secret://` URI convention per §2.7; EncryptedSecret with the no-recursion `key_ref` constraint per §6.2. Verified: well-formed JSON Schema 2020-12 (passes `Draft202012Validator.check_schema`); all 10 example `.uacp` artifacts validate against it cleanly. Semantic checks beyond pure structural validation (bidirectional path-parameter coverage, embedded-credential detection, `$ref` locality, inferred-source provenance completeness) remain the implementation's responsibility per §3.10 — JSON Schema can't express them naturally, and the prototype's `spec/schema.py` already covers them. **The spec freeze finalization**: SPEC.md rewritten as a real entry-point document (status block + reading order with `**Stable**` flips for all eight stage docs + conformance summary section + schema pointer + versioning + license/governance + post-freeze changes policy); `docs/00-primer.md` Status flipped to `v1.0.0` with the per-stage status table inside the primer flipped to Stable; README Status section flipped likewise; `docs/02-authentication.md` §2.0 In-scope updated from "nine `Authentication Method`s" to "ten" with the §2.10 row added; `docs/07-versioning.md` §7.5's v1.0 freeze subsection rewritten in past tense (was prospective gate text). GOVERNANCE.md gains a "When v2 becomes worth pursuing" section with concrete thresholds (3+ deprecated identifiers, structural inadequacy, adoption-driven feature cluster); maintainer judgment remains the deciding mechanism per §7.6, thresholds are signals not bright lines. **The MCP composition adapter** at `prototype/python/src/uacp_prototype/mcp/server.py` is a thin server that walks a directory of `.uacp` files and exposes each operation as an MCP tool. Tool name = `<provider>_<operation_id>` where provider derives from the artifact's parent directory (or its `name` field when the artifact lives directly at the configured root); dots and slashes normalize to underscores per the OpenAI/Anthropic `^[a-zA-Z0-9_-]{1,128}$` regex; 128-char cap enforced by truncation. Tool input schema = a single JSON object with optional `path_params` / `query` / `body` / `extra_headers` sub-objects matching `DispatchClient.dispatch`'s keyword API. Tool execution dispatches through the existing UACP runtime (`auth/`, `dispatch/`, `lifecycle/`, `security/`) so security and dispatch invariants apply transparently to MCP-side callers. The default credential resolver covers all five wired auth methods (oauth2_authorization_code, x-oauth2-workspace, api_key_header/query, aws_sigv4, session_cookie) reading from local-keyring + env vars per a documented scheme. Tests inject custom `dispatch_factory` callables to mock at the dispatch boundary. Run as `python -m uacp_prototype.mcp --uacp-dir <path>`. Against the prototype's `examples/` tree the server advertises **10 tools** (one per operation across the five validated providers): google_gmail_users_messages_send, google_calendar_events_list, slack_chat_postmessage, slack_conversations_list, aws_s3_getobject, aws_s3_listobjectsv2, github_repos_get, github_repos_list_for_user, notebooklm_notebooklm_list_notebooks, notebooklm_notebooklm_send_chat_message. **Adds the `mcp_integration` pytest marker** (separate from `integration` because MCP-side tests use the mcp SDK but no real provider credentials). Adds `mcp>=1.0` to dependencies. **Test count after Stage 9**: **434 unit tests** passing (414 Stage 8e baseline + 20 MCP server unit tests covering normalize_tool_name + load_directory + tool-definition derivation + dispatch routing + error propagation), **33 integration tests** deselected by default (5 Google + 5 Slack + 5 AWS + 5 GitHub + 5 NotebookLM + 8 NEW MCP integration), 0 failures. Run wall ~0.56s. **The 8 MCP integration tests** at `tests/integration/test_mcp_composition.py` pair the prototype's `UACPServer` with the MCP SDK's `ClientSession` over in-process memory streams (`mcp.shared.memory.create_connected_server_and_client_session` — no subprocess spawning required, both run in the same event loop with paired send/receive streams). Tests cover: server_advertises_expected_tool_count (10 tools), server_advertises_expected_tool_names (exact name set), tool_schemas_match_uacp_request_shapes (§3.2 shape derivation), tool_call_returns_dispatch_success (DispatchSuccess envelope round-trips through MCP TextContent), tool_call_arguments_pass_through_to_dispatch (path_params/query/body/extra_headers split correctly), dispatch_error_propagates_canonically_through_mcp (§4.6 status/code/message/details preserved), credential_resolution_failure_surfaces_through_mcp (factory-raised exceptions become a credential_resolution_failed envelope), multiple_providers_dispatch_independently (single MCP server routes to multiple .uacp artifacts cleanly). All 8 pass under `pytest -m mcp_integration`. **The release notes draft** at `RELEASE-v1.0.0.md` (1396 words) covers the design phase summary, prototype validation (Stage 8: five providers, four amendments tied to their provider rounds), the Stage 9 MCP composition validation summary, what's stable in v1.0, what's deferred to v1.x, what's deferred to v2, the spec-changes-after-freeze policy, links + acknowledgments. Operator pastes into GitHub's release form when publishing. **The prototype README's MCP Composition section** documents the `python -m uacp_prototype.mcp --uacp-dir examples/` startup, the 10 advertised tools across the five providers, the operation-to-tool naming rules, a sample `.claude/mcp.json` config for connecting Claude Code, the env-var conventions for credential resolution per auth method, and instructions for running the mcp_integration test suite. **The `v1.0.0` git tag** is created locally as a signed annotated tag pointing at this memory commit; **NOT pushed** per the brief's hard rule. Operator pushes manually. **Spec gaps surfaced**: ZERO. Stage 9 was a freeze + composition validation session; no new spec content was added beyond the v1.0 marker. The §3.10 placeholder `$schema` URL was the only outstanding spec item gating freeze, and Stage 9's commit 1 closes it. **`docs/open-questions.md` Q1 (`$schema` canonical URL)** flipped to **Resolved** with the canonical URL captured, the `schemas/uacp.json` artifact-creation commit hash recorded, and the per-minor-tag URL strategy documented. Q2 (trademark policy) and Q3 (operator-side public release) remain Open per the brief's hard rule that the operator handles repo visibility. **Hard rules honored**: spec freeze happened BEFORE MCP composition (Commit 1 + 2 are spec freeze, Commit 3-4 are MCP work, Commit 5 is docs); v1.0.0 tag is the freeze marker (created locally on Commit 6, not pushed); the canonical `$schema` URL choice is GitHub raw with a v1.0.0 tag pin (the brief noted "URL won't resolve until the repo is public" — this is now operator action item Q3); did not push the repo or tag; MCP server wrapper is additive (no refactoring of existing prototype code beyond the auth method imports the server pulls in). **Stage 8 + Stage 9 status**: BOTH **complete**. v1.0.0 frozen. **Spec repo state after this session**: tip will be the memory commit (Commit 6 of this session); five new commits + this memory commit + the v1.0.0 tag. New tree under `prototype/python/`: `mcp/__init__.py`, `mcp/__main__.py`, `mcp/server.py`, `tests/unit/test_mcp_server.py`, `tests/integration/__init__.py`, `tests/integration/test_mcp_composition.py`. New tree at repo root: `schemas/uacp.json`, `RELEASE-v1.0.0.md`. SPEC.md is now a real document (was a 25-line placeholder pointing at the per-stage docs). **Operator action items at session end**: (1) push the local commits to `origin/main`; (2) push the `v1.0.0` tag (`git push origin v1.0.0`); (3) make the GitHub repo public if not already (the canonical `$schema` URL doesn't resolve externally until then); (4) create the GitHub release using the drafted notes from `RELEASE-v1.0.0.md`.

---

**Prior session (2026-05-04)**: **Stage 8e — session_cookie auth + LLM-inference path + NotebookLM prototype shipped (4 spec-repo commits + memory commit).** Final per-provider Stage 8 session, and the most distinctive single spec contribution of the prototype-validation arc. Closes the §2.1 auth registry by adding `session_cookie` (the tenth method) for grey-zone provider integrations where the operator accepts ToS-violation risk to access an undocumented API. Closes §3.8 by implementing the LLM-inferred schema authoring path with the mandatory user-review gate. Validates both against Google NotebookLM, which has no public API and no published OpenAPI — the cleanest possible exercise of "spec ingestion under operator review." **Spec repo commit chain on top of `30f3ea4` (Stage 8d memory tip)**: `7d1067d feat(prototype): implement session_cookie authentication` → `7ca10b6 feat(prototype): implement LLM-inferred schema authoring (§3.8)` → `13ae3ee fix(spec): §2.10 — register session_cookie as v1.x auth method` → `162dfe1 feat(prototype): NotebookLM examples + session_cookie dispatch wiring`. Tip is `162dfe1` until this memory commit lands. **The session's central find**: §2.1's nine registered methods covered every authenticated-API path UACP should ever need for clean providers, but the long tail of agent-useful surfaces (NotebookLM, Notion's pre-API era equivalents, internal tools, captive-portal SaaS) sits behind browser-equivalent session replay. Without spec backing, every implementation would invent its own grey-zone auth shape and likely drift on ToS posture, capture-flow safety, and audit logging. Path A again — additive `session_cookie` registration in §2.1 with a new §2.10 subsection codifying shape + conformance + the `tos_acknowledged: true` (literal boolean, enforced at spec-loader level) gate. The `MUST surface ToS-violation-risk warning to the user` rule lands in §2.10 conformance and §6.7 trust-model. **The spec-loader-level enforcement** is the load-bearing detail — Pydantic `model_validator(mode="after")` rejects any `session_cookie` artifact whose `tos_acknowledged` isn't the literal Python `True` (string `"true"`, integer `1`, boolean `False`, missing field — all rejected). Tests verify each rejection path explicitly. **Session-cookie auth implemented from scratch** using only stdlib + httpx: `parse_storage_state` (Playwright `storage_state.json` shape), `filter_cookies_for_url` (RFC 6265 §5.1.3 domain matching + §5.1.4 path matching + secure-flag enforcement), `format_cookie_header`, `inject_cookies` (whitelist filtering with explicit `cookie_names`), `refresh_csrf` (configurable extraction via JSONPath subset OR regex). `SessionCookieMethod` adapter implements the `AuthMethod` protocol; emits the §2.10 ToS warning via stdlib logging on construction (so a single `dispatch.client` instance logs once per session, not per dispatch). **CSRF auto-refresh** wired into `dispatch.client.py` per §2.10: on 401/403/419 the dispatcher attempts one CSRF refresh against the configured `refresh_url` (with current cookies, since cookies are valid and only the CSRF token is stale), extracts the fresh token via the configured path, stores it on the client, **re-applies auth so the retry carries the freshly-derived `X-Same-Domain` header rather than the stale cookie-derived one**, and continues the loop once. The one-retry-per-dispatch cap prevents loops; if refresh fails, the original 401 is surfaced as `auth_expired`. **§6.6 audit logging** emits `INFO risk=tos_violation_potential` on every dispatch through a session_cookie connection — production deployments can route this through their audit log pipeline. **§3.8 LLM-inferred schema authoring** lands at `connections/ingest_nl.py` replacing the stub: pluggable `LLMCallable` Protocol with a default `build_default_openrouter_callable` wrapping the OpenRouter API; `infer_from_description` returns an `InferenceDraft` carrying `InferredOperation`s + `InferenceProvenance` with `reviewed_at` empty; `confirm_and_persist` REQUIRES `approved=True` else raises `InferenceNotApprovedError`; `refine_inference` preserves operation ids per the §3.8 refinement rule. The `SYSTEM_PROMPT` specifies UACP v1.x operation shape rigorously; markdown fence stripping in `_parse_llm_response` handles the typical LLM output shape. **Both NotebookLM `.uacp` files** are produced through this path: `examples/notebooklm/list-notebooks.uacp` (POST `/_/NotebookLmRpcs/data/batchexecute?rpcids=wXbhsf` — RPC id reverse-engineered from `notebooklm-py`) and `examples/notebooklm/send-chat-message.uacp` (rpcids=BfMzVf, `idempotency=not_idempotent`). Both carry `source.type=inferred`, `source.model=anthropic/claude-haiku-4.5`, `source.reviewed_at` populated, the operator's natural-language description preserved verbatim, and `tos_acknowledged: true`. Cookie whitelist matches Google's session set: `SID`, `HSID`, `SSID`, `APISID`, `SAPISID`, `__Secure-1PSID`, `__Secure-3PSID`. CSRF block carries `header_name: X-Same-Domain`, `cookie_name: _csrf_token`, `refresh_url`, `extraction_path: $.token`. **Aggregate test count after Stage 8e**: **414 unit tests passing** (339 Stage 8d baseline + 38 session_cookie auth + 26 ingest_nl + 4 spec-loader session_cookie tests + 7 NotebookLM end-to-end mocks), **25 integration tests deselected by default** (5 Google + 5 Slack + 5 AWS + 5 GitHub + 5 NotebookLM new), 0 failures. Run wall ~0.46s. **Ten `.uacp` files validating** through `spec.loader.load()`: gmail-send, google-calendar-list, chat-postMessage, conversations-list, s3-getobject, s3-listobjectsv2, repos-get, repos-list-for-user, list-notebooks, send-chat-message. **Spec gaps surfaced**: ONE (§2.10 session_cookie registration). Closed via path A. **No new entries to `docs/open-questions.md`** — session_cookie shape now spec-anchored at §2.10; LLM-inference review-gate spec-anchored at §3.8; tos_acknowledged enforcement is at the spec-loader level not a gap. **Hard rules honored**: did not change Google, Slack, AWS, or GitHub code; `tos_acknowledged: true` enforced at the spec loader level (Pydantic `model_validator`); §2.10 ToS-warning surfaces through user-facing channel (stdlib logging during prototype, audit pipeline in production); LLM-inference path REFUSES to persist without `approved=True`; Playwright capture flow is intentionally stubbed (CLI subcommand `uacp capture-storage-state` prints the manual recipe and exits 1, with the README documenting both the stub and the manual capture path). NotebookLM `.uacp` files validate cleanly through the unmodified spec loader after the §2.10 amendment. Did not push the repo. **The `dispatch/client.py` re-apply-auth-after-refresh fix** is the load-bearing dispatch detail — initial implementation just `continue`d after a successful CSRF refresh, but the headers dict still carried the original X-Same-Domain value. Extracted `_apply_auth(op, url, headers, body)` so both the initial dispatch and the post-refresh retry path share the same auth-apply logic; verified end-to-end via the `test_list_notebooks_401_triggers_csrf_refresh_and_retry` test that asserts the second request carries the NEW token. **Spec repo state after this session**: tip `162dfe1` on `main` (will become this memory commit's parent); four new commits + this memory commit. New tree under `prototype/python/`: `auth/session_cookie.py` (new), `connections/ingest_nl.py` (replaces stub), `examples/notebooklm/{list-notebooks,send-chat-message}.uacp` (new), `tests/unit/{test_session_cookie,test_ingest_nl,test_end_to_end_notebooklm_mock}.py` (new), `tests/providers/test_notebooklm.py` (new), `cli.py` (gains `capture-storage-state` subcommand), `dispatch/client.py` (gains `_apply_auth` + CSRF refresh path + audit emit + csrf_state injection), `spec/models.py` (registered set includes session_cookie + tos_acknowledged validator). `docs/02-authentication.md` gains §2.10 + §2.1 registry row + §2.9 conformance row. `docs/06-security.md` gains §6.1 session-cookie threat-model row + §6.7 session-cookie trust-model subsection.

**Stage 8 status**: **complete**. All five per-provider sessions (Google → Slack → AWS → GitHub → NotebookLM) shipped with one spec amendment each. Stage 9 prototype freeze is the next gate.

---

**Prior session (2026-05-04)**: **Stage 8d — GitHub PAT prototype + RFC 8288 link-header pagination strengthening shipped (4 spec-repo commits + memory commit).** Fourth per-provider session. The shortest of the prototype-validation arc: `api_key_header` is the simplest auth shape in UACP's registry (just inject a header), so the meaty work was tightening the §3.4 `link_header` pagination implementation against RFC 8288's edge cases, which GitHub's paginated endpoints exercise thoroughly. **Spec repo commit chain on top of `feb8ae9` (Stage 8c memory tip)**: `3cf63e7 feat(prototype): implement API-key authentication (header + query)` → `5ccd6e0 feat(prototype): implement RFC 8288 link-header pagination at dispatch` → `290490e fix(spec): §3.4 — RFC 8288 conformance rules for link_header pagination` → `d7b9a8c feat(prototype): GitHub repos.get + repos.listForUser .uacp + integration tests`. Tip is `d7b9a8c` until this memory commit lands. **The session's central find**: §3.4's `link_header` subsection was a one-paragraph punt — it said "follow RFC 8288's `Link` header" without enumerating which RFC 8288 conformance rules a Conforming Implementation MUST satisfy. The Stage 8a parser handled the typical `<url>; rel="next"` case but glossed over rel case-insensitivity, multiple rels per entry, multi-Link-header concatenation, relative-URI resolution, link parameters beyond rel, and the comma-inside-brackets edge case. Without spec backing, every Conforming Implementation would have to figure these out independently and likely diverge on real providers like GitHub. Path A again — additive, clarifying amendment to §3.4 enumerating seven RFC 8288 conformance rules. Non-breaking per Principle 6 / §7.2; the rules clarify behavior that was implicit but unspecified, and existing implementations that already handled the typical Link shape correctly continue to work. **`api_key_header` covers GitHub's three PAT formats trivially** — `ghp_` (classic), `github_pat_` (fine-grained), `gho_` (OAuth-app-issued) all use the same `Authorization: Bearer <token>` wire shape and UACP doesn't distinguish them. The artifact says "API key in Authorization header"; the runtime injects whatever the secret store resolved. Format-agnostic by design, which is exactly the simplification the brief expected. **`api_key_query` (§2.4.2) implemented alongside** with the §2.4.2 disrecommendation warning emitted on every apply — the spec prescribes the warning surface, the prototype emits via stdlib logging so the dispatcher's audit pipeline can capture it. **Strengthened RFC 8288 parser** in `dispatch/pagination.py` handles: (1) string OR list[str] input (HTTP allows multiple Link headers); (2) comma-separated entry splitting that respects angle brackets and quoted values (so commas inside URIs or quoted titles aren't treated as entry separators); (3) rel case-insensitivity per RFC 8288 §3.3; (4) space-separated multiple rels per entry per §3.3; (5) multiple link parameters per entry (title, type, hreflang) with only `rel` consumed for pagination; (6) quoted vs unquoted parameter values; (7) relative URI resolution per §3.4 against the request URL (with subsequent advances resolving against the most-recent URL, not the artifact base). Three helper functions exposed for testing: `_split_link_entries`, `_parse_link_entry`, `_is_absolute_uri`. **GitHub `.uacp` files** at `examples/github/`: `repos-get.uacp` (GET /repos/{owner}/{repo} with `Authorization: Bearer ` prefix and GitHub's recommended `Accept: application/vnd.github+json` + `X-GitHub-Api-Version: 2022-11-28` default headers) and `repos-list-for-user.uacp` (GET /users/{username}/repos with link_header pagination, all standard GitHub query params, and the Link response header declared in the response.headers schema per §3.3 best practice). **Aggregate test count after Stage 8d**: **339 unit tests passing** (290 Stage 8c baseline + 17 api_key + 26 link-header parser + 6 GitHub end-to-end mocks), **20 integration tests deselected by default** (5 Google + 5 Slack + 5 AWS + 5 GitHub), 0 failures. Run wall ~0.45s. **Eight `.uacp` files validating** through `spec.loader.load()`: gmail-send, google-calendar-list, chat-postMessage, conversations-list, s3-getobject, s3-listobjectsv2, repos-get, repos-list-for-user. **Spec gaps surfaced**: ONE (§3.4 link_header conformance rules) — closed via path A, additive amendment to §3.4. The §2.4.1 "header injection cleanly expressed" probe the brief flagged was investigated and confirmed clean — `{header_name, header_prefix, key_ref}` already covers Authorization+Bearer, X-API-Key+empty, and the long tail; no §2.4.1 amendment needed. The "multiple PAT format support stays clean" probe was investigated and confirmed clean — the artifact's three-field shape doesn't care about the key's internal format, which is exactly the right abstraction. **No new entries to `docs/open-questions.md`** — link_header conformance rules now spec-anchored; api_key shape needs no amendment; PAT format-agnosticism is a spec property not a gap. **Hard rules honored**: did not change Google, Slack, or AWS code; did not relitigate Stage 0-7 spec content beyond the §3.4 link_header amendment which was filed as `fix(spec):`; GitHub `.uacp` files validate cleanly through `spec.loader.load()`; did not push the repo; link-header pagination code is in `dispatch/pagination.py` (the generic location) not a GitHub-specific module — Atlassian, GitLab, and many other REST APIs that use the same RFC 8288 pattern get the same parser without changes. **Spec repo state after this session**: tip `d7b9a8c` on `main` (will become this memory commit's parent); four new commits + this memory commit. New tree under `prototype/python/`: `auth/api_key.py` (new), `examples/github/{repos-get,repos-list-for-user}.uacp` (new), `tests/unit/{test_api_key,test_pagination_link_header,test_end_to_end_github_mock}.py` (new), `tests/providers/test_github.py` (new). `dispatch/pagination.py` upgraded with the RFC 8288 parser. `docs/03-schema.md` gains an RFC 8288 conformance-rules block in §3.4's link_header subsection.

---

**Prior session (2026-05-04)**: **Stage 8c — AWS S3 / SigV4 prototype + XML / binary response handling shipped (4 spec-repo commits + memory commit).** Third per-provider session. Validates UACP against AWS S3 — a provider whose authentication shape is structurally unlike OAuth (per-request signing, no flow, triple credential refs) and whose response shape diverges from JSON (binary content for GetObject, XML for ListObjectsV2). The pluggable-auth abstraction holds; the §3.3 body-format amendment closes the second clean gap of the prototype-validation arc. **Spec repo commit chain on top of `8de529f` (Stage 8b memory tip)**: `c1596d4 feat(prototype): implement AWS SigV4 signing` → `441f44d feat(prototype): handle XML and binary response bodies at dispatch` → `5d72616 fix(spec): §3.3 — body format discriminator (json / xml / binary / text)` → `29bbc39 feat(prototype): S3 GetObject + ListObjectsV2 .uacp + integration tests`. Tip is `29bbc39` until this memory commit lands. **The session's central find**: §3.3 modelled response bodies as JSON-only, but real APIs return binary (S3 GetObject) and XML (S3 metadata APIs, SOAP-flavored services, legacy enterprise). Without spec backing, every implementation would diverge on decoding. Path A again — additive amendment introducing an optional `format` field on response body objects (json / xml / binary / text). Non-breaking per Principle 6 / §7.2. **SigV4 implementation written from scratch** using only `hashlib` + `hmac` from stdlib. No `boto3` / `botocore` dependency — UACP's whole point is owning the auth implementation. The §2.5.1 wire shape already supported triple-credential refs (`access_key_ref` + `secret_key_ref` + optional `session_token_ref`) — that "spec gap" the brief flagged turned out to not exist; the spec already handled it cleanly per-method. **AWS-published test vectors verified byte-for-byte**: the canonical IAM ListUsers worked example from AWS's General Reference reproduces with kSigning=`c4afb1cc5771d871...86da6ed3c154a4b9`, canonical request, string-to-sign, and final signature `5d672d79c15b13162d9279b0855cfba6789a8edb4c82c400e06b5924a6f2b5d7` all matching exactly. Edge cases handled and tested: empty body uses SHA-256 of empty string (NOT UNSIGNED-PAYLOAD; that's only for streaming uploads — flagged as Stage 9 consideration); query-string multi-value sort by (name, value); header whitespace trimming + sequential-whitespace collapse; STS session token in `x-amz-security-token` participating in signed headers; S3 single-encoding vs non-S3 double-encoding; default-port stripping in Host (`:443` for HTTPS); `x-amz-content-sha256` REQUIRED-and-signed only for S3 (matching AWS's IAM test vector exactly which omits it for non-S3 services). **XML→dict conversion** in `dispatch/body_format.py` uses stdlib `xml.etree.ElementTree`; documented conversion rules: namespace-stripped element names → keys; `@`-prefixed attributes; repeated child tags → lists; leaf text → bare string; `#text` for mixed content. The same JSONPath subset from §3.4 traverses the post-conversion dict, so cursor pagination over XML (S3's `$.ListBucketResult.NextContinuationToken`) works without any new pagination machinery. **AWS `.uacp` files** at `examples/aws/`: `s3-getobject.uacp` (GET /{bucket}/{key} with `format=binary` response, path-style addressing for full bucket-name compatibility) and `s3-listobjectsv2.uacp` (GET /{bucket}?list-type=2&... with `format=xml` response and cursor pagination via `$.ListBucketResult.NextContinuationToken`). **Aggregate test count after Stage 8c**: **290 unit tests passing** (241 Stage 8b baseline + 23 SigV4 + 21 XML/body-format + 5 AWS end-to-end mocks), **15 integration tests deselected by default** (5 Google + 5 Slack + 5 AWS), 0 failures. Run wall ~0.40s. **Six `.uacp` files validating** through `spec.loader.load()`: gmail-send, google-calendar-list, chat-postMessage, conversations-list, s3-getobject, s3-listobjectsv2. **Spec gaps surfaced**: ONE (body format) — closed via path A, additive amendment to §3.3. The §2.7 "single secret_ref per method" gap the brief flagged was investigated and found to not exist — §2.5.1 already declares triple-credential refs cleanly per-method, and §2.7 only specifies the URI shape. The §2.5.1 "AWS published spec exactly" punt the brief flagged was investigated and found to be appropriate; AWS's spec covers per-service variants (S3 vs non-S3) and the implementation matches the test vectors. Streaming-upload UNSIGNED-PAYLOAD is flagged as a Stage 9 consideration; v1 dispatch §4.7 covers streaming responses but not requests, and S3's typical use cases here (GetObject + ListObjectsV2) don't need it. **No new entries to `docs/open-questions.md`** — body-format gap closed in spec; SigV4 implementation matches AWS test vectors; UNSIGNED-PAYLOAD streaming-upload flagged as Stage 9 design but not currently a v1 concern. **Hard rules honored**: did not depend on boto3 or botocore (SigV4 from scratch); did not change Google or Slack code (SigV4 is purely additive in `auth/aws_sigv4.py` replacing the stub); .uacp files validate cleanly through `spec.loader.load()` after the §3.3 format-field amendment; did not push the repo. AWS's published SigV4 test suite anchored the implementation correctness — the IAM ListUsers worked example is the load-bearing test vector. **Spec repo state after this session**: tip `29bbc39` on `main` (will become the memory-commit's parent); four new commits + this memory commit. New tree under `prototype/python/`: `auth/aws_sigv4.py` (replaces the stub), `dispatch/body_format.py`, `examples/aws/{s3-getobject,s3-listobjectsv2}.uacp`, `tests/unit/{test_aws_sigv4,test_xml_response,test_end_to_end_aws_mock}.py`, `tests/providers/test_aws.py`. `docs/03-schema.md` gains a new "Body format" subsection in §3.3.

---

**Prior session (2026-05-04)**: **Stage 8b — Slack OAuth prototype + body-predicate envelope handling shipped (4 spec-repo commits).** Second per-provider session validates UACP against Slack and lands the first `fix(spec):` of the prototype-validation arc — adding `failure_predicate` to §3.3 + §4.6 to handle the 200-with-ok=false envelope shape Slack (and GraphQL-flavored / many enterprise APIs) use. **Spec repo commit chain on top of `fce9de7` (the migration tip)**: `b19526e feat(prototype): implement Slack workspace-scoped OAuth` → `619078b feat(prototype): handle response envelopes ({ok: bool}) at dispatch` → `412f7a1 fix(spec): §3.3 + §4.6 — body-predicate failure detection` → `9146d2c feat(prototype): Slack chat.postMessage + conversations.list .uacp + integration tests`. Tip is `9146d2c`. **The session's distinguishing find**: Slack's HTTP-200-with-ok=false envelope is a clean spec gap that path A (additive spec change) closes cleanly. The body-predicate machinery is intentionally minimal in v1.0: a JSONPath path + an equals literal + optional code_path / message_path. Predicate is opt-in per response entry; absent predicate retains existing status-only success/failure semantics. The amendment is non-breaking per Principle 6 / §7.2 — adding an optional field to ResponseEntry. **Slack's three OAuth divergences from RFC 6749 vanilla** are all handled in `auth/oauth2_workspace.py` as a separate AuthMethod (RFC 6749 vanilla `oauth2_authcode.py` left intact): (1) `scope=` carries bot scopes + `user_scope=` carries user scopes (Slack uses comma-separated values, not space; both parameters at the authorization endpoint, RFC 6749 defines only one); (2) two tokens in the response — bot `xoxb-...` at `access_token` + user `xoxp-...` at `authed_user.access_token`, plus workspace identity (team_id, bot_user_id, app_id) — `WorkspaceTokens` dataclass holds all of it; (3) tokens don't expire by default (Slack added optional rotation in 2021), so `bot_expires_at = None` for non-rotating installs and the §5.2 refresh-window logic short-circuits cleanly. PKCE primitives (`PKCEPair`, `generate_pkce_pair`) and `OAuth2Error` re-used from `oauth2_authcode.py` — no duplication. Per §7.3 in-development extensions, `.uacp` files use `x-oauth2-workspace` as the method identifier until the spec promotes it. **Slack `.uacp` files** at `examples/slack/`: `chat-postMessage.uacp` (POST /api/chat.postMessage with body-predicate detecting Slack's 200+ok=false envelope) and `conversations-list.uacp` (GET /api/conversations.list with cursor pagination via the deeply-nested `$.response_metadata.next_cursor` JSONPath — exercises §3.4's subset against nesting beyond a top-level field, validating that the existing pagination implementation handles it cleanly). **Aggregate test count after Stage 8b**: **241 unit tests passing** (188 Stage 8a baseline + 23 oauth2_workspace + 25 envelope + 5 Slack end-to-end mocks), **10 integration tests deselected by default** (5 Google from Stage 8a + 5 Slack new), 0 failures. Run wall ~0.39s. **Four `.uacp` files validating** through `spec.loader.load()`: gmail-send, google-calendar-list, chat-postMessage, conversations-list — all clean. **Spec gaps surfaced**: ONE — the body-predicate gap, closed in this session via path A. **Path-A rationale recorded**: the change is small (~600 words across §3.3 + §4.6), additive (optional field on ResponseEntry, optional evaluation step in dispatch), non-breaking per Principle 6 / §7.2, and exactly what Stage 8 prototype validation is supposed to surface — providers like Slack with HTTP-200-with-error-body envelopes are common enough that without spec backing every implementation would diverge on detection. Path B (Slack-specific workaround) was rejected because a fix this small shouldn't land as undocumented divergence. Path C (status-code re-mapping at dispatch time) was rejected for being lossy (the dispatcher's canonical `status` field MUST stay faithful to wire per §4.6 audit posture). **No new entries to `docs/open-questions.md`** — the body-predicate gap is closed in spec; Slack's `user_scope` parameter is handled via the §7.3 `x-` namespace until promotion; no new operator action items. **Hard rules honored**: did not change Google's existing prototype code (only `spec/models.py` ResponseEntry got a new field, and `dispatch/client.py` got the envelope hook in the 2xx branch — those are additive and the 188 Stage 8a tests still pass unchanged); did not relitigate Stage 0-7 spec content beyond the §3.3 + §4.6 amendment which was filed as `fix(spec):`; Slack `.uacp` files validate through the unmodified spec loader (after the §3.3 amendment lands in models.py); did not push either repo. The `prototype/python/` work from Stage 8a remains intact. **Spec repo state after this session**: tip `9146d2c` on `main`, four new commits. New tree under `prototype/python/`: `auth/oauth2_workspace.py` + `dispatch/envelope.py` + `examples/slack/{chat-postMessage,conversations-list}.uacp` + `tests/unit/{test_oauth2_workspace,test_envelope,test_end_to_end_slack_mock}.py` + `tests/providers/test_slack.py`. `docs/03-schema.md` gains a new "Body-predicate failure detection" subsection in §3.3; `docs/04-dispatch.md` gains a new "Body-predicate evaluation" subsection in §4.6. **AVA monorepo**: untouched this session — UACP is self-documenting now per the prior migration.

## Phase

**v1.1.0 released 2026-05-05; Stage 11.x capture pipeline complete (capture in 11.1 + synthesis in 11.2). Stage 10 (production reference implementation in AVA) is the next gate.**

- Stages 0-7 (Primer, Principles, Authentication, Schema, Dispatch, Lifecycle, Security, Versioning) are all written, committed, and marked **Stable** in `SPEC.md` + `docs/00-primer.md`. v1.0.0 amendments: §3.3 (body-predicate, body-format), §3.4 (RFC 8288 link-header conformance), §4.6 (body-predicate evaluation), §2.1+§2.10 (session_cookie + ToS gate). v1.1.0 amendments: §3.12 (session-capture schema source) + §3.9/§3.11 cross-references; §4.10 (pluggable transport backends) + §4.9 cross-reference. All amendments are non-breaking per Principle 6 / §7.2.
- Stage 8 — prototype validation across providers — **complete**.
- Stage 9 — spec freeze + MCP composition validation — **complete**.
- Stage 11.0 — v1.1 spec amendment + Scrapling transport — **complete**.
- Stage 11.1 — browser capture infrastructure — **complete**.
- **Stage 11.2 — operation synthesis from captures — complete.** Deterministic analyzer + LLM synthesis pipeline + interactive review CLI + spec-loader-level enforcement of §3.12 mandatory-user-review shipped. End-to-end verified against the httpbin capture from 11.1: capture → analyze → synthesize → review → persist → validate cleanly. Spec content unchanged — Stage 11.2 is implementation against the frozen v1.1.0 spec.
- Stage 10 (production reference implementation in AVA's `backend/services/connections-broker/`) is the terminal stage. The full §3.12 capture pipeline is now demonstrable end-to-end as the contract Stage 10 builds against.

## What's done

- **Spec docs (Stages 0-7)** — eight design documents under `docs/`. §3.3 + §4.6 amended in Stage 8b with the body-predicate machinery (additive, non-breaking). §3.3 amended again in Stage 8c with the body-format discriminator (additive, non-breaking). §3.4 amended in Stage 8d with RFC 8288 conformance rules for the link_header pagination pattern (additive clarification, non-breaking).
- **Repo scaffolding** — README, SPEC, GOVERNANCE, CONTRIBUTING, CODE_OF_CONDUCT, NOTICE, LICENSE (Apache 2.0); CLAUDE.md / AGENTS.md / ORCHESTRATION.md at repo root.
- **Stage 8a Google prototype** — Python reference implementation at `prototype/python/`. Modules: `spec/` (loader + validator + pydantic models including FailurePredicate as of Stage 8b), `auth/oauth2_authcode.py` (PKCE S256, four-step flow), `dispatch/` (HTTPS dispatch + retry + pagination + envelope handler + canonical error shape), `lifecycle/` (state machine + lazy refresh + atomic rotation), `security/` (AES-256-GCM envelope encryption + filesystem-simulated `local-keyring`), `connections/ingest_openapi.py` (canonical OpenAPI 3.x + Google discovery doc). Two example `.uacp` files (`examples/google/`).
- **Stage 8b Slack prototype** — `auth/oauth2_workspace.py` (Slack's workspace-scoped OAuth flavor with bot + user tokens, optional rotation, comma-separated scopes), `dispatch/envelope.py` (body-predicate evaluation + provider-error-string-to-canonical-code mapping for Slack's well-known errors). Two example `.uacp` files (`examples/slack/`).
- **Stage 8c AWS S3 prototype** — `auth/aws_sigv4.py` (full SigV4 from scratch using stdlib `hashlib` + `hmac`; no boto3 / botocore; verified against AWS-published IAM ListUsers test vector byte-for-byte; handles S3 vs non-S3 URL encoding, STS session tokens, query-string multi-value sort, header whitespace trimming, default-port stripping in Host, x-amz-content-sha256 S3-only inclusion), `dispatch/body_format.py` (XML→dict via stdlib `xml.etree.ElementTree`, format-aware response decoding for json / xml / binary / text). Two example `.uacp` files (`examples/aws/`).
- **Stage 8d GitHub prototype** — `auth/api_key.py` (`api_key_header` for the Authorization+Bearer / X-API-Key / Token-prefix patterns; `api_key_query` with §2.4.2 disrecommendation warning emitted via stdlib logging). `dispatch/pagination.py` upgraded with a robust RFC 8288 link-header parser handling case-insensitive rel matching, multi-rel entries, multi-Link-header concatenation, relative-URI resolution, link parameters beyond rel, comma-in-brackets / comma-in-quotes edge cases. Two example `.uacp` files (`examples/github/`).
- **Stage 8e NotebookLM prototype** — `auth/session_cookie.py` (RFC 6265 cookie filtering + Playwright `storage_state.json` parsing + CSRF refresh via JSONPath/regex extraction; emits §2.10 ToS warning on construction). `connections/ingest_nl.py` (replaces the stub; pluggable `LLMCallable` Protocol + default OpenRouter wrapper; `confirm_and_persist` requires explicit `approved=True` per §3.8 mandatory user-review gate). `dispatch/client.py` extended with `_apply_auth` extraction so the §2.10 CSRF refresh-and-retry path re-applies auth correctly; one-retry-per-dispatch cap; §6.6 audit emit on every dispatch. `spec/models.py` enforces `tos_acknowledged: true` (literal Python boolean) at load time. `cli.py` gains `capture-storage-state` subcommand (stubbed — prints the manual Playwright capture recipe). Two example `.uacp` files (`examples/notebooklm/`) produced through the §3.8 inference path.
- **Stub plan** — only Stage 9+ work remains stubbed: `auth/custom.py` (deferred — the v1.0 registered set was demonstrated complete by Stage 8e's session_cookie close-out; the §2.6 `custom` extension point is exercised in production by Stage 10 if a real provider needs it); `connections/ingest_curl.py` (kept as a stub since the §3.7 curl-paste validation path was not exercised by any of the five provider sessions — Stage 9+ if a provider with no published OpenAPI lands); `cli.py capture-storage-state` (stubbed — prints the manual Playwright recipe; Stage 9+ replaces with an interactive Playwright launch); secret-store resolvers `vault` and `aws-secrets-manager` are stubs in `security/secrets.py` (Stage 9+; the AWS Secrets Manager resolver lives in §6.2's secret-store registry). `auth/aws_sigv4.py` no longer stubbed (Stage 8c); `auth/api_key.py` no longer stubbed (Stage 8d); `auth/session_cookie.py` and `connections/ingest_nl.py` no longer stubbed (Stage 8e).
- **AVA-side anchor** — `docs/adr/ADR-036-uacp.md` in the AVA monorepo records the AVA architectural decision to build UACP. ADR-036 stays in AVA; UACP's `docs/memory/` is the implementation/session record.

## What's next

1. **Operator action items from Stage 11.2.** Push the three prototype commits + this memory commit to `origin/main` (`4c066f1` analyzer, `9c69c1f` synthesis pipeline, `c0dd056` CLI + review surface, then this memory commit on top of `42012cc`). No new tag — Stage 11.2 is implementation against the frozen v1.1.0 spec. (Stage 9 / 11.0 / 11.1 prerequisites remain valid if not yet done: push v1.0.0/v1.1.0 commits + tags + make repo public.)
2. **Stage 11.3+ — Synthesis-quality validation against real LLMs.** Stage 11.2 ships the structural pipeline + a recorded mock-LLM demo. Validating synthesis QUALITY against e.g. `anthropic/claude-haiku-4.5` on real captures (Slack / NotebookLM / Notion / arbitrary grey-zone services) is the next quality gate; the architecture lets operators swap the LLMCallable, but no API key was configured in this session. Companion follow-ups: auth-block-from-capture inference (the analyzer surfaces auth_artifacts but the CLI doesn't yet draft an authentication block — currently operators add it before `uacp validate`); GraphQL-shaped POST-only capture support (the §1.2 clustering rules are REST-shaped); session-capable Scrapling browser API for capture-side fingerprint matching.
3. **Stage 10 — Production reference implementation in AVA.** The prototype's design migrates into a Ring 3 service at `backend/services/connections-broker/` in the AVA monorepo. Coexists with the curated `IntegrationProvider` set per ADR-035; UACP handles the long tail. The full v1.1.0 + Stage 11.x capture pipeline is now demonstrable end-to-end as the contract Stage 10 builds against.
4. **Post-freeze deliverables tracked separately**: trademark + conformance-mark policy (open-question Q2); SigV4 streaming-upload UNSIGNED-PAYLOAD if streaming-request semantics are added to §4.7; interactive Playwright `capture-storage-state` to replace the §2.10 stub; `auth/custom.py` per §2.6 if a real provider need surfaces; backend identifier registry at §4.10 (deferred from v1.1 explicitly); session-capable Scrapling browser API for capture-side fingerprint matching. These land as v1.x non-breaking additions or Stage 10 implementation work as appropriate.

## Blockers

None.

Q1 (`$schema` canonical URL) is **resolved** as of Stage 9 — pinned at the v1.0.0 git tag, JSON Schema artifact landed at `schemas/uacp.json`, all 10 example artifacts validate cleanly. Q2 (trademark + conformance-mark policy) is post-freeze deliverable, not a Stage 10 blocker. Q3 (operator-side public release) is the operator-action item the freeze depends on for external resolution; the spec freeze itself doesn't depend on it.

The 33 deselected integration tests (25 provider integration + 8 MCP integration) require operator-supplied credentials per the setup in `prototype/python/README.md`. The MCP integration tests don't need real provider credentials — they pair the server with the MCP SDK over in-process memory streams; running them is just `pytest -m mcp_integration`.
